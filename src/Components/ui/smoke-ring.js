// src/Components/ui/smoke-ring.js
// Thin wrapper around the paper-design SmokeRing shader so the rest of the app
// imports it from one place (mirrors the shadcn "components/ui" convention,
// adapted for this JavaScript CRA project).
import { SmokeRing, smokeRingPresets } from "@paper-design/shaders-react";

export { SmokeRing, smokeRingPresets };

export default SmokeRing;
